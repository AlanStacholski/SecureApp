# Versões fixadas — decisão de segurança:
# dependências com versão exata garantem builds reproduzíveis
# e evitam que uma atualização automática introduza uma vulnerabilidade

# Framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# Banco de dados
sqlalchemy==2.0.30
asyncpg==0.29.0          # driver async para PostgreSQL
alembic==1.13.1          # migrações de banco

# Autenticação e segurança
python-jose[cryptography]==3.4.0   # JWT
passlib[bcrypt]==1.7.4             # hashing de senha
bcrypt==4.1.3
python-multipart==0.0.26           # form data para login

# Validação
pydantic==2.7.1
pydantic-settings==2.3.0          # carregamento de .env tipado

# Rate limiting
slowapi==0.1.9

# Utilitários
python-dotenv==1.2.2
starlette==0.47.2